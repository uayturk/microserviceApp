spring:
  application:
    name: eureka-server #Burdan şunu anlıyor: config-server'da eureka-server.yml diye bir dosyaya gidip bakmam lazım. Bu isimde bir config var ise onunla başlıcam.
  cloud:
    config:
      profile: native
      uri: http://localhost:8500